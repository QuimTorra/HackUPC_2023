import { Text, View } from "react-native";
import React, { useState} from 'react';

export default function Grafiques() {

    return (
        <View>
            <Text>
                Grafiques
            </Text>
        </View>
    );
}